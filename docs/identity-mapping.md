# Identity Mapping

The identity mapper records neutral prologue behavior and returns exactly three
`IdentityFragment` choices.

The first demo action labels are:

- `observe`
- `help`
- `hide`

The mapper stores tendency as timeline data but does not show personality scores
or story-specific interpretation.
