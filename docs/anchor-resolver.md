# Anchor Resolver

Anchor events are fixed structural points. Variants are selected by conditions.

Supported operators:

- `>`
- `>=`
- `<`
- `<=`
- `==`
- `!=`

All conditions on a variant must pass. Multiple variants may be selected in the
first implementation, which keeps behavior transparent for tests and demos.
