# @lacuna-engine/ui-kit

Small shared UI primitives for the demo apps.

The first pass keeps components plain and content-neutral. Product-specific
visual systems can replace or extend these primitives without changing engine
packages.
