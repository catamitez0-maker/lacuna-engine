# @lacuna-engine/identity-mapper

Maps neutral prologue actions into three generic `IdentityFragment` choices.

The mapper records tendencies as data, but it does not expose personality
scores or embed story-specific interpretation. Replace or extend this package
when a World Pack needs richer identity logic.
