# @lacuna-engine/observer-archive

Builds observer reports after a pulse settles.

The archive records behavior, traces, state deltas, and selected variants. It
does not narrate or intervene; future AI narrator layers should consume reports
rather than replace them.
