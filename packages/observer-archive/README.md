# @lacuna-engine/observer-archive

Builds observer reports after a pulse settles.

The archive records behavior, traces, state deltas, selected variants, and rule
audit summaries. It does not narrate or intervene; future AI narrator layers
should consume reports rather than replace them.
